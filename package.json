{
  "name": "jie-album-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check main.js",
    "preview": "python3 -m http.server 8788 --bind 127.0.0.1",
    "deploy": "npx wrangler pages deploy . --project-name=jie"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
